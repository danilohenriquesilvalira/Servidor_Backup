# Banco de Dados
DB_NAME=rls_backup
DB_USER=rls
DB_PASS=rls_secret_2024

# JWT - TROQUE EM PRODUÇÃO!
JWT_SECRET=rls-super-secret-jwt-key-change-this-in-production

# Senha padrão dos usuários (será criada no primeiro boot)
DEFAULT_PASSWORD=rls@2024

# Portas expostas do host (mude se houver conflito)
FRONTEND_PORT=3009
BACKEND_PORT=3010

# URLs (use o IP do servidor em produção)
FRONTEND_URL=http://localhost:3009
VITE_API_URL=http://localhost:3010
